flask_backend/*.log
flask_backend/data/*.db
# IE source xlsx (large — kept locally, not pushed to GitHub)
data/source_files/IE_xlsx/
data/source_files/IE_xlsx_Jun/
# Backup DB files (local only)
flask_backend/backup/*.db
# Watchdog single-instance lock (runtime, local only)
flask_backend/watchdog.lock
# 暫存腳本
_fix_ws2.py
_inspect_ds04.py
_inspect_ds04_out.txt
_migrate_042.py
_migrate_044.py
_parse_ds04_out.txt
_query_actual.py
_query_actual2.py
_update_ws.py
_verify_sum.py
flask_backend/atlas.db
flask_backend/data/atlas.db
flask_backend/*.db
# Stress-test DB (generated, ~50MB — local only, never push)
flask_backend/tests/*.db
flask_backend/tests/*.db-wal
flask_backend/tests/*.db-shm
flask_backend/Run
# ── 系統運行產物（不追蹤，避免造成「本地有改動」擋更新）──────────────────────────
# 測試輸出、匯出 xlsx、log 等
flask_backend/test_output/
*.log
# 臨時測試腳本
flask_backend/check_ie.py

# 根目錄臨時測試腳本/截圖（本地產物，不追蹤，避免擋更新鍵）
/test_login.py
/test_login_result.png

# Node (real_click_test.js gate deps — Task X)
node_modules/

# ── 備份產物（Task FOUNDATION）：DB 的 -wal/-shm 旁檔與自動備份/排程 log 一律不追蹤 ──
flask_backend/backup/*.db-wal
flask_backend/backup/*.db-shm
flask_backend/data/*.db-wal
flask_backend/data/*.db-shm
flask_backend/data/auto_backup/
flask_backend/backup_task.log
# ── 入規27：真實生產資料永不進 git（2026-07-15，中樞裁決，永久標準）──────────────
# IE-VER 對帳/遷移隔離副本：DB 檔 **與報告 .md 一律不進 git**。
# 報告含鞋型/ART料號/標時/實際人數/工序明細＝真實生產資料，靠本機檔案傳遞，不靠 repo。
# （舊規「報告 .md 要追蹤」已於 2026-07-15 廢止；f9126aa/fa0906e 已改寫移除）
real_db_for_recon/

# 真實資料報告的其他慣用落點（一律本機）
**/real_db_for_recon/
*_recon_report*.md
*_eolr_evidence*.md
*_blocker_*.md
__pycache__/
