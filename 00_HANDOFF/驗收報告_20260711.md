# 背景批次驗收報告（2026-07-11 起，Code 機無人值守）

---

# ══ 第三批（2026-07-13）Task F / G / H / I / H-1 / J / K / L / N / O ══

### Task H-2 — 連刀下拉新增 6
- `INTERLOCK_OPTIONS` 1/2/4/8/16 → **1/2/4/6/8/16**（遞增，默認 1）；27_WORKING_RULES §二①-H 同步。
- 匯入：`/api/ie/import/apply` 按 DB 欄名複製 `interlock_cut`，**無值白名單**（後端 add_row 僅 int 轉型）→ 無需改。
- Playwright（隔離 atlas_taskH2.db）全 PASS：下拉 `['1','2','4','6','8','16']` 含 6 且順序正確；
  層1件11刀1 連刀1→標時 **39600**、連刀6→**6600**(÷6)、理論 **220**；未存層數改連刀不丟(7)；read_only 0 select/0 input。
- 證據：`test_screenshots/taskH2_interlock6/task_H2_result.json`。

### Task O-1 — 移除 ART 選單缺漏修復（群組聯集）
- **根因**：列表列＝model 群組（EOLR 60/120 兩 header 合併，`g.arts` 取聯集），但 `removeArtFromModel(headerId)`
  只取單一 header 的 `rec.arts` → 掛在另一 header 的 ART 不出現在選單。
- **修法**：① 選單資料源改為「該 model 群組全部 header 的 ART 聯集」（用 `normKey(model_name)` 找同群組
  sibling records，建 `art → [header_ids]` 映射，同一 ART 多 header 列一次）。② `doRemoveArt` 帶該 ART **實際所屬
  header_id**（可多個→逐 header 移除），非入口點 header。③「最後一個」以群組聯集計數；某 header 清空→沿用後端級聯刪，
  另一 EOLR header 不受影響。
- **Playwright（隔離 atlas_taskO1.db，seed DUALTEST：h201 eolr60=ARTX-60ONLY / h202 eolr120=ARTX-120A,B）4 情境全 PASS**：
  ① 由入口 h202 開選單 → 列出**聯集 3 筆**含 ARTX-60ONLY（data-hids=201）。
  ② 移除 ARTX-60ONLY（掛 h201）→ **h201 級聯刪、h202 仍在且剩 2 arts**。
  ③ 降到剩 1 art → 最後一個警告迴歸。④ Task O 不存在 ART → `ok:false`（寬鬆比對/rowcount 迴歸）。
- 證據：`test_screenshots/taskO1_group/task_O1_result.json` + O1 截圖。

### Task O — 移除 ART 靜默失敗 bug 修復
- **根因**：`remove_art_from_header` 用 `DELETE ... WHERE art=? AND header_id=?`（**精確字串比對**），
  且**不檢查 rowcount** → 大小寫/空白不符或 ART 不存在時刪到 0 列，仍 `return ok:True` →
  前端顯示「成功」但資料仍在（靜默失敗）。
- **修法**：
  ① 後端比對放寬 `UPPER(TRIM(art))=UPPER(TRIM(?))`；`cur.rowcount==0` → `ok:false, error='此鞋型中找不到該 ART'`。
     其餘級聯（空 header 連帶刪 ob_epph/ob_rows/ob_header）不動。
  ② 前端 `removeArtFromModel` 由 `prompt('輸入 ART')` 改為**選擇視窗**：列出該 header 現有全部 ART
     （來源 `allRecords[id].arts`），點選其一→確認文案帶該 ART→送 API；只有一個 ART 時顯示
     「移除將刪除整個鞋型」警告。
- **同類掃描（item 3）**：ie_interface 其餘 prompt 手打入口只剩「新增 ART」「改 EOLR」（任務指定排除）；
  兩者皆非「打字→精確比對 DELETE→靜默」模式（新增=INSERT；改 EOLR 值來自驗證後的 60/120、header_id 來自列非手打）。
  **除 remove_art 外無同類 bug**。
- **Playwright（隔離 atlas_taskO.db）5 情境全 PASS**：
  ① 選擇視窗列出 13 個 ART；點選髒 ART → 13→12（列表即時更新）。
  ② 髒資料 `'  DirtyCase_ZZ  '`（含空白/大小寫）經 UI 點選成功移除（後端 loose-match）。
  ③ 移除最後一個 ART（header 69 單 ART）→ 顯示「刪除整個鞋型」警告 → 整鞋型消失。
  ④ API 直打不存在 ART → `ok:false`＋錯誤訊息（不再靜默）。
  ⑤ read_only/editor：無 ··· 選單、`/api/ie/remove_art` → 403（remove_art＝`_require_manager`，
     editor 亦無此權，非「僅限有權鞋型」——ART 管理屬 manager/admin 範疇）。
- 證據：`test_screenshots/taskO_removeart/task_O_result.json` + O1 截圖。

# ══ 第三批（2026-07-13）Task F / G / H / I / H-1 / J / K / L / N ══

### Task N — 裁斷重算改 admin 管理頁（取代 cmd 腳本派發）
原則寫進 27_WORKING_RULES §七：「正式庫維運操作一律做成 admin 管理頁按鈕，預覽→確認→自動備份→可還原；
不派 cmd 到生產機；ME129 現場只使用不維運。」
- 新頁 `/admin/recalc-cutting`（admin 限定，入口在 ie_interface 頭部僅 admin 顯示）。三步：① 預覽(dry-run)
  ② 執行(二次確認→自動整庫備份→重算) ③ 還原(列備份一鍵還原)。**執行中鎖**擋並發。
- 後端 `database.recalc_cutting_preview/apply/backups/rollback`（復用 recalc 腳本邏輯；腳本保留備援）。
  備份用 SQLite online backup；還原用 ATTACH 逐列還原 standard_time（避免整檔覆蓋的檔案鎖）。
- **Playwright（隔離 atlas_taskN.db，13 列模擬 ×1.1）5 情境全 PASS**：
  ① admin：預覽 in_scope=13/would_change=13/排除=7965；執行 changed=13、39600-config 列 std **43560→39600**；
     還原→43560。**預覽筆數＝實改筆數**。
  ② 執行中鎖：並發 2 個 apply → **[200 changed=13, 409 busy]**（不雙重執行）。
  ③④⑤ manager/editor/read_only：入口隱藏、頁面 403、apply API 403。
- 證據：`test_screenshots/taskN_recalc/task_N_result.json` + N1 截圖。

# ══ 第三批（2026-07-13）Task F / G / H / I / H-1 / J / K / L ══

各自獨立 commit + push。隔離副本 `atlas_taskF.db` / `atlas_taskH.db`（由 atlas.db 複製），**不碰正式 DB / ME129**。

| Task | 內容 | 結果 | commit |
|---|---|---|---|
| F | 裁斷 standard_time DB 重算 ×1.0（方案a） | ✅ 隔離 E2E 全 PASS；ME129 派發腳本已交（正式庫待 Jim 派發） | `c307278` |
| G | IE 產能彙總表**定案 36 欄** + UI 導出入口 | ✅ 定案+入口完成；產能/人數值 BLOCKED（解鎖條件已寫死） | `14f517d` |
| H | IE 裁斷段界面改版（4 項定案） | ✅ Playwright 4 情境全 PASS + read_only 403 迴歸 | `8e700b3` |
| I | 前端角色感知渲染（read_only/無權 editor 全灰） | ✅ Playwright 5 情境全 PASS（含 read_only 403） | `e58a1e2` |
| H-1 | 連刀欄移到「層數」左邊（只動顯示順序） | ✅ Playwright 3 情境全 PASS + 匯入帶 interlock DB 驗證 | `6ba4602` |
| J | STF 段所有區塊「實際人數」→「EOLR=190 實際人數」(三語) | ✅ Playwright 4 情境全 PASS | `c29e53d` |
| K | 設備種類管理頁（manager/admin） | ✅ Playwright 6 情境全 PASS（含 409/403） | `50e9d30` |
| L | STF 段欄位標準化（向 Assembly 看齊） | ✅ Playwright 3 情境全 PASS | （本次） |

### Task L — STF 段欄位標準化（向 Assembly 看齊）
規格寫進 27_WORKING_RULES §二「STF 段欄位標準化」。**純前端**（DB/公式/導出取值不變）。
- STF 段所有區塊改用 assembly 相同 8 欄組＋公式（`標時=正常×(1+寬放/100)`，寬放默認10；生産目標/理論照 assembly）。
  實作在共用定義層：`getZoneCols`/`renderCell`/`_rowStd`/add-row 的 `stitching||assembly` 分支併入 `stf`，
  移除舊 STF 特例（打粗/照射手工標時、貼底×1.1、水洗僅實際、defaultAllow）。
- **舊資料相容**：缺 normal_time → 顯示存 standard_time；補正常時間後公式勝出。不回填不重算。
- **Task J 疊加**：STF 實際人數表頭仍「EOLR=190 實際人數」（未被蓋回）。
- **item 5 確認**：編制表/36欄 STF MP `訂單×TCT÷3600÷222` 的 TCT = `ip.standard_time`（`get_bianche_dept` line ~2869），
  Task L 未動 DB 與後端讀取 → **取值不變**。
- **Playwright（隔離 atlas_taskL.db）3 情境全 PASS**：
  ① 欄位/公式：STF 4 區塊表頭欄數 **9＝assembly 9**；seeded 打粗(正100寬10)→標時 **110**/生産目標 32.7/理論 3.7；
     照射(僅存 std88)→顯示 **88**(舊資料 fallback)；貼底(正50+存 std88)→**55**(補正常時間後公式勝出)；Task J 表頭全保留。
  ② 真實資料(header 160，未鎖)：打粗 20→22、照射 11.2→12.32、水洗 2→2.2、貼底 12→13.2 全部 std=正常×1.1 **相符**。
  ③ read_only 全灰：STF input/select=0、表頭仍「EOLR=190 實際人數」。
- 證據：`test_screenshots/taskL_stf/task_L_result.json` + L1/L2/L3 截圖。

### Task K — 設備種類管理頁（權限 manager/admin）
規格寫進 27_WORKING_RULES §十「設備種類管理」。**權限 2026-07-13 Jim 定案改為 manager＋admin**。
- 新頁 `/admin/equipment-types`（`equipment_types_admin.html`），入口鈕在 `ie_interface.html` 頭部（帳號管理旁），
  **admin/manager 顯示**；因在 ie_interface（非細表）→ 不受 Task I `applyReadOnlyDOM` 全灰影響 → manager 可點。
  **manager 的 IE 工序仍全灰唯讀不變**——此處只開「設備種類選項維護」一個功能給 manager。
- 後端：`/admin/equipment-types` 頁 + `/api/equipment_types/admin`(GET,含停用+引用數) + POST/PUT/DELETE
  一律 `_require_manager()`；改名/刪除被引用 → **409**（`referenced,ref_count`）。GET `/api/equipment_types`
  （下拉來源）維持任何登入者可讀、只回 active。
- **引用鎖定**：`_equip_ref_count` 查 `ie_process.equipment_type=name`；已引用→前端改名/刪除鈕 `disabled`＋
  顯示「已被 N 道使用🔒」；未引用可改名/刪除（刪除二次確認）。停用/啟用、排序不受引用限制。
- **Playwright（隔離 atlas_taskK.db）6 情境全 PASS**：
  ① manager(test_mgr)：/ie 見入口、開頁不 denied、UI 新增成功。
  ② 已引用列(三針針車機TEST ref=1)：改名/刪除鈕 disabled+顯示引用數；硬打 API PUT改名/DELETE → **409**。
  ③ editor(test_editor)：入口隱藏、頁面 403、寫入 API 403。
  ④ read_only(tongcai)：入口隱藏、頁面 403、寫入 API 403。
  ⑤ 下拉（admin 檢視 STF）：新增 DROPTEST→細表下拉出現；停用→重載後下拉消失。
  ⑥ 亂點：連續停用/啟用 4 次 0 pageError。
- 證據：`test_screenshots/taskK_equip/task_K_result.json` + K1 截圖。

### Task J — STF 段「實際人數」欄改名（全區塊，共用定義層）
規格寫進 27_WORKING_RULES §二「STF 段『實際人數』欄改名」。
- 施工在 **STF 段共用表頭定義層** `SEG_COL_LABELS.stf.actual_operators`（renderZoneCard 讀取），
  **不逐區硬寫** → STF 現有與未來新增區塊自動繼承。三語 data-* 屬性 + setLang 即時切換。
- **只改顯示名稱**：欄位 key（actual_operators）、DB、公式、36欄導出、合併鈕行為全不動；只 seg==='stf' 生效。
- **Playwright（隔離 atlas_taskJ.db，header 32）4 情境全 PASS**：
  ① STF **5 個區塊全數**（打粗/照射/水洗/貼底/水蜘蛛）表頭＝「EOLR=190 實際人數」；
     切越南文→全數「EOLR=190 Số người thực tế」；切回中文→還原。
  ② 其他段未波及：stitching/assembly 表頭仍「實際人數」、cutting 無 EOLR=190 字樣。
  ③ 填值互動後表頭仍為改名值（迴歸）。
  ④ read_only 視圖：5 區塊表頭同樣改名、input/select=0。
- 證據：`test_screenshots/taskJ_stf/task_J_result.json` + J1/J4 截圖。

### Task H-1 — 連刀欄位移位（連刀 → 層數 左邊）
- 三語表頭（中/英/越）+ 資料列 + 新增列 同步移位：材料類別｜部件名稱｜**連刀**｜層數｜件數｜刀數/H｜標時…
- **公式/DB/36欄導出不變**（總計列 4 空格位置無關，未動）。
- **匯入查核（item 3）**：裁斷區匯入 = `/api/ie/import/apply` **按 DB 欄名** source→target 列複製（**非按顯示位置**）
  → 欄序調整**不影響匯入**。另補：複製時一併帶 `interlock_cut`（原先漏帶→預設1）。
- **Playwright（隔離 atlas_taskH1.db）3 情境全 PASS**：
  ① admin：連刀在層數左邊（cell idx 3<4、表頭 連刀 在 層數 前）；連刀1→標時 **39600**、連刀4→**9900**（÷連刀迴歸）；
     未存層數7 改連刀不丟。
  ② read_only：0 select、連刀顯示為文字且在新位置（連刀 idx2 < 層數 idx3）。
  ③ 匯入 h32裁斷機→h3：ok、13 道；DB 直查 h3 裁斷機 `interlock_cut` 含 {1,2} → **連刀帶入成功**。
- 證據：`test_screenshots/taskH1_interlock/task_H1_result.json` + 截圖。

### Task I — 前端角色感知渲染（Jim 2026-07-12：不能執行的操作不該顯示）
規格已寫進 27_WORKING_RULES §十「角色感知渲染」。
- **細表 `ie_cell_detail.html`**：`applyReadOnlyDOM()` 強化 — input/select→**灰底純文字**（含連刀 select→文字）、
  操作鈕（插/×/合/內聯確認/匯入/＋觸發列）**display:none**、頂部 儲存▼/送審/取消審核/設為鎖定版隱藏；
  `isEditor`(送審顯示) 加 `CAN_EDIT` 條件。read_only 用 `_ie_locked_only` 只看鎖定版。
- **`eolr_settings.html`**：加 `/api/me` 取角色，read_only → EOLR 下拉改**純文字**。
- **`allocation.html`**：加 `_canEdit=(admin/unit_user)`，非撥人編輯者 → 勾選框 `disabled` + `toggle()` 防線。
- **`ie_cutting.html`**：純檢視頁（只有搜尋/篩選，無資料編輯元素）→ **無需改**（逐頁查核結果）。
- **Playwright（隔離 atlas_taskH.db，每角色真登入）5 情境全 PASS**：
  ① read_only(tongcai) 細表：input=0 / 連刀 select=0 / 可見操作鈕=0 / 儲存▼ 隱藏、數值可讀；
     直打 `/api/ie/cell/save` 寫 interlock_cut → **403**。
  ② editor(data_entry) 無權鞋型(h3)：input=0 全灰；有權鞋型(h32)：input=243 / 連刀 select=13 / 操作鈕=122、可編輯。
  ③ admin(jim) 不受影響：input=243 / 連刀 select=13 / 儲存▼ 可見（迴歸）。
  ④ eolr：read_only select=0/文字 228；admin select=228。
  ⑤ allocation：read_only 勾選框 107 個 **全禁用**。
- **⚠ 自行決定（manager 渲染）**：後端 `_can_edit_ie` 現行 manager 對 IE 工序=唯讀（can_edit=false），
  故 manager 亦渲染全灰。Task I「保留後端 403 防線不動」→ **未改後端**；與 Task I 期望「manager 不受影響」
  不一致處，留待 Jim 定：manager 是否應可編輯 IE（屬另案後端變更）。admin 完全不受影響。
- 證據：`test_screenshots/taskI_roles/task_I_result.json` + I1~I3 截圖。

### Task H — 裁斷段界面改版（Jim 2026-07-12 定案四項）
規格已寫進 27_WORKING_RULES §二「①-H 裁斷段界面改版」。
1. **ATOM/EMMA 精簡**：type B→C，只剩 5 欄（層/件/標時/理論/裁機要求人數）＋材料/名稱；
   印线/削皮/贴补强/涂边/热压 從 UI 移除、總計排除；**DB 欄位與既有資料保留不刪**。
2. **裁斷機新增「連刀」欄**：下拉 1/2/4/8/16 默認 1；`ie_process` 加 `interlock_cut INTEGER DEFAULT 1`
   （既有 8295 列全補 1）；公式 `3600÷刀÷層×件×1.0÷連刀`，理論連動；手工列不套；連刀切換即時
   重算+存值**不 reload**（迴歸 Task D flush）；匯入/導出取除完連刀值（recalc 腳本亦 ÷連刀，默認1向下相容），36 欄不變。
3. **新增「裁斷手工」區**（type M）：①裁斷機 後、ATOM 前，編號順延；欄位 流程名稱/標時/理論(公式灰)/要求人數；
   插/刪/+/總計行；計入 cutting 段總計（後端 hand_total + bianche CUTTING_ZONES 均已納入）。
- **Playwright（隔離 atlas_taskH.db，header 32）4 情境全 PASS**：
  ① 連刀 default=1；層1件11刀1→標時 **39600**/理論 **1320**；連刀→4→**9900**/**330**；未存層數不丟；回1→39600。
  ② ATOM/EMMA 資料列 **8 欄**、表頭無後製欄、舊資料載入 0 error。
  ③ 裁斷手工 新增→標時60→理論 **2**、總計 std60/理論2、資料列 **5 欄**。
  ④ 亂點：連刀快速連切 6 次 + 空行插/刪 **0 pageError**、空名稱正確 alert。
- **對抗（後端雙層防線迴歸）**：read_only(tongcai) 直接打 `/api/ie/cell/save` 寫 interlock_cut → **403**。
- 前端**角色感知全灰渲染**（含連刀 select、裁斷手工區 read_only）於 **Task I** 統一處理。
- 證據：`test_screenshots/taskH_cutting/task_H_result.json` + H1~H3 截圖。

### Task F — 裁斷 standard_time ×1.0（方案a）
- 只重算「公式型裁斷機列」：`zone='裁斷機'` 且 刀/層/件三要素齊全、非 manual、未刪除。
  公式 `3600÷刀÷層×件×1.0`，與前端 `fmCutA()` 一致。**ATOM/EMMA/Laser/YINGHUI/移印/轉印手工區、
  裁斷機區內手工列 一律不動**。
- **bianche 維持讀 DB 值，不改即時算**（鎖定版＝快照語意）。理論人數各處即時由 std 導出 → 重算即連動。
- 隔離 E2E：模擬 ME129 ×1.1 → `--dry-run`(13列) → `--apply`(改13列+自動整庫備份) →
  DB-vs-細表 **0 mismatch** → bianche 理論 **MATCH**(0.6316) → `rollback` **還原**。全 PASS。
- 交付：`recalc_cutting_x10.py`＋`rollback_cutting_x10.py`（ME129 可直接跑，dry-run→apply→備份→可還原）、
  `Task_F_執行說明_20260713.md`、`test_screenshots/cutting_recalc/task_F_result.json`。
- **本機無真實 cutting 資料 → 實際受影響筆數以 ME129 `--dry-run` 為準；正式庫本批未動。**

### Task G — IE 產能彙總表定案 36 欄
- **定案：36 全欄**，與 廠務編製自動計算.xlsx「數據源-IE标准」sheet 逐欄一致。
  **「29 欄」出自已丟失規格檔 → 作廢**（handoff 相關記錄一併標註作廢）。
- **取值來源定案**：IE 鎖定版 + offline 撥人（照 39_VERSION_CONTROL_DESIGN / 28_BIANCHE_SPEC 主流程）。
- **值維持 BLOCKED**（產能/人數欄留空、不臆造）。**解鎖條件（二擇一，已寫進碼註解與導出說明）**：
  ① 在 ME129 執行（有真實 IE 鎖定資料）；或 ② 把 IE 來源 xlsx 匯入 Code 機後填值驗收。
- **UI 導出入口已補**：ie_interface.html 工具列「⬇ IE產能導出」按鈕，**admin/manager 限定**
  （後端 `/api/ie/export/capacity` 加 `_require_manager()` 強制擋）。無資料時導出仍出 36 欄表頭
  （值欄空），**不報錯**；權限不足才提示。
- Flask test_client 驗證（隔離庫）：未登入 **401** / read_only **403** / manager **200**（27.7KB xlsx，36 欄 197 列）。

---

# ══ 第二批（2026-07-12）Task 0 / 1 / D / E ══

四項各自獨立 commit + push，全部完成。隔離 E2E 庫 `atlas_e2e.db`（不碰正式 DB / ME129）。

| Task | 內容 | 結果 | commit |
|---|---|---|---|
| 0 | 定案存檔（公式×1.0 / 併列不比對FOB / 21三線導向） | ✅ | `fdeab0d` |
| 1 | cutting 公式 ×1.1 → ×1.0 | ✅ PASS | `4e5e8ef` |
| D | flushPendingEdits() 通用修法 | ✅ 6情境PASS | `af2b4b0` |
| E | IE 產能彙總表導出（規格回源） | ✅ 36欄逐欄符合（→ Task G 定案 36 欄，「29欄」作廢；數值 BLOCKED 解鎖條件已寫死） | `15efddd` |

### Task 1 — cutting ×1.0
- 改前端 4 處裁斷機計算點（fmCutA/thCA/_rowStd/recalcCuttingStdByPid）+ test_zone_totals.py：`×1.1→×1.0`。
- **不動** STF貼底/針車寬放率 `normal_time×1.1`（不同公式）。
- Playwright：層1件11刀1 → 標時 **39600** / 理論 **1320**（×1.1 會是 43560/1452）→ **PASS**。
- ⚠ **DB 資料一致性（先報告、未動資料）**：裁斷 `standard_time` 在 DB 是**匯入時存自來源 Excel**
  的值（`ie_reimport_cutting_32.py: std_time=tf(d.get(8))`），非本系統即時算。細表顯示是即時
  ×1.0，但 **bianche 裁斷MP 讀 DB 的 standard_time（Excel×1.1 基礎）→ 兩者會不一致**。
  **重算方案（待 Jim 決定，未執行）**：(a) 重算 DB cutting `standard_time = 3600÷刀÷層×件×1.0`
  （凡 layers/qty/cph 齊全的列）；或 (b) 改 bianche 裁斷改即時算（層/件/刀×1.0）不讀存死值。
  本機無真實 cutting 匯入資料，受影響筆數需在 ME129 上查。

### Task D — flushPendingEdits()（修法A強化版）
- 新增 `flushPendingEdits()`：`blur` 進行中輸入框 → 未存編輯進 EDITS → 送後端存；
  **任一失敗→保留不丟 + alert，且不執行後續動作**。
- 接進所有觸發 `loadSegment()` 的進入點：合併 / 解除合併 / 更新合併人數 / 插入(modal+cutting/generic inline) /
  刪除 / 切製程tab（`switchSeg`）/ 切版本（`switchStage`，保留攔截、改先存不丟）/ 另存新版（`newStage`）。
- Playwright 隔離 6 情境（各獨立 header）**全 PASS**：
  未存層數 8 → 合併後仍在；切tab(6)/刪(5)/插(4) 皆保留；**後端500→值不丟(3)+合併中止+提示**；
  已存資料合併 rowspan=3、群組列欄位對齊迴歸正確。

### Task E — IE 產能彙總表導出（回源）
- 規格檔 `IE产能导入格式规格_20260625.md` 確認不存在（已不再找）。
- **回源**：真實 IE 產能來源 = `data/source_files/IE編製/廠務編製自動計算.xlsx` 的 **`數據源-IE标准`** sheet，
  表頭 **36 欄（非 29）**。欄名（回源讀取，非臆造）：輔助值/季度/Layout时产能/Layout类型/Article/
  鞋型名称/标准PPH/(贴底/电脑针车/折边自动裁断/同材共裁/移印YingHui/贴补强转印/打粗水洗UV照射)CT/
  StockfittingEOLR/Stockfitting(贴底/打粗水洗UV照射/STF)人数/(裁断/针车/成型/贴底/同材共裁/自动化/
  电脑针车/STF)标准(产能+人数)/CSA标准人数/C2B标准人数。
- 實作 `/api/ie/export/capacity`（`database.py IE_CAPACITY_COLS + export_ie_capacity()`）照真實 36 欄輸出。
- Playwright：導出 200 / 6.2KB，**36 欄與來源逐欄 EXACT MATCH**（0 mismatch）；可推得欄
  （季度/Article/鞋型名称/StockfittingEOLR）已填；產能/人數欄留空（不臆造）。
- ✅ **已由 Task G（2026-07-13）定案**：(1) **36 全欄**（「29 欄子集」作廢）；
  (2) 取值來源＝**IE 鎖定版 SUM.C2B + offline 撥人**；數值仍 BLOCKED，解鎖條件已寫死
  （ME129 執行 / IE 來源 xlsx 進 Code 機）。詳見本報告頂部「第三批 Task G」。
- 證據：`test_screenshots/ie_capacity/`（導出檔 + 逐欄比對 json）。

### 本批自行決定清單（無人值守）
| # | 哪一步 | 問題 | 採用 | 為什麼 |
|---|---|---|---|---|
| 12 | 41/42 交接檔 | Jim 說在 GitHub，實際 pull 後不存在；貼文又截斷（41 止於 §3.3、42 未到） | 不寫半截 handoff；先跑實質批次，請 Jim 補貼全文 | 「原封不動」與截斷矛盾；不臆造交接內容 |
| 13 | Task1 範圍 | `1.1` 有兩類（裁斷公式 vs 針車/STF寬放率） | 只改裁斷 4 處，寬放率不動 | Jim 只定案裁斷機公式；不誤傷其他段 |
| 14 | Task1 DB | 裁斷 standard_time 存死(Excel) vs 即時 | 報告不一致+兩個重算方案，不動資料 | 規則「先報告不動資料」 |
| 15 | Task D 切版本 | 「保留攔截確認」與「接上flush」語意衝突 | 保留 confirm，改為先存到目前版本再切；存失敗不切 | 最符「不丟資料」本意 |
| 16 | Task D 重現資料 | 需畫面有 cutting 列 | 用最小 UI fixture（隔離庫，非業務資料） | UI-bug 驗證需列；非造假業務結果 |
| 17 | Task E 欄數 | 實際來源 36 欄 ≠ 規格所稱 29 | 照真實 36 欄輸出，把 29↔36 當 Jim 決策 | 「回源、不臆造」；29 無來源佐證 |
| 18 | Task E 值 | 無真實 IE 資料、來源型體與本系統不同 | 只填可確定欄、其餘留空、標待驗收 | 不臆造數值；誠實標 BLOCKED |
| 19 | Task E UI 入口 | 要不要加按鈕 | 先只出 API 端點（可直接用），不加未定案 UI | 36↔29 未定前不落地介面 |

---


執行：Claude Code（無人值守，依 25_CODE_UNATTENDED_RULES.md）
環境：Code 機本機開發環境，**完全不碰 ME129 生產機**。
DB：獨立 E2E 測試庫 `flask_backend/data/atlas_e2e.db`（透過 `ATLAS_DB` 環境變數隔離，
    從不觸碰既有 `atlas.db`）。Flask 以 `serve.py`（waitress）跑在 **localhost:5057**。
Playwright：Node 版 chromium（裝在 repo 外 `C:\pw-e2e`，腳本副本存 `flask_backend/tests/e2e_bianche_playwright.js`）。

---

## ⚠ 全域前提：本機缺 IE 來源資料（影響 Task A Step5/6、Task B）

- `data/source_files/IE_xlsx/`（623MB IE 來源，IE_MANIFEST.md 列為「本機存放，未推 GitHub」）
  **在本 Code 機上不存在**。故本機 DB 沒有任何 IE 鎖定版工序資料（`ie_process` 空）。
- 依無人值守規則「不自己造測試資料」：**不捏造 IE 數字**。
- 影響：凡是需要 IE 鎖定版標時的驗證（編制表 MP 手算、勾選表部件、裁斷合併真資料重現）
  一律**記錄為 BLOCKED**，不以假資料充數。
- 例外：為讓 /bianche 不因「資料表不存在」而 500，建立了**空的** `ie_process/ie_stage/
  ie_process_group`（0 筆，schema-only，非業務資料）以驗證「缺 IE→紅底不擋單」的程式路徑。

---

# Task A｜編制表全流程 Playwright E2E 驗收

三個本輪決策①②③已寫入 `28_BIANCHE_SPEC.md`。截圖存 `test_screenshots/bianche_e2e/`。

## 各步 PASS/FAIL

| Step | 項目 | 結果 | 說明 |
|---|---|---|---|
| 1 | /ds04 上傳解析 | ✅ PASS | 見下 |
| 2 | 覆蓋＋變更記錄（決策①，本輪實作） | ✅ PASS | 見下 |
| 3 | 明細表確認鎖定 → 不可編輯 | ✅ PASS | 鎖定後改單被擋 |
| 4 | /eolr-settings | ✅ PASS | 繼承預設120、可改、eolrDiv 公式確認 |
| 5 | /allocation 勾選表 | ⛔ BLOCKED | prefill 0 筆（無 IE 工序可分配）|
| 6 | /bianche 編制表計算 | ⚠ PARTIAL | 決策③已驗；MP 手算 BLOCKED（無 IE）|
| 7 | 導出 | ✅ PASS | 只有 CSA/OCS/RB/QC，無核對 tab |

### Step 1｜/ds04 上傳解析 ✅
> 註：DS-04「上傳」在現行系統是 **CLI 匯入**（`python import_ds04.py`，來源硬編在
> `parse_ds04.py` 指向 repo 內 `data/source_files/廠務編制/2026年6月份正式进度表 5 30.xlsx`）。
> **無網頁上傳路由**（對比 DS-01/02 有 `/api/dsNN/upload`）。E2E 以 CLI 匯入 + Playwright 讀 /ds04 畫面驗證。

- LEAN 組名解析（去加/組、中文數字轉阿拉伯）：33 個 LEAN 正確
  （含 1A/2C/7B/10C/**11A1/11A2/11B1/11B2/11C**/12A/12C/12D）。
- 雙訂單格式數量相加：`MF2606KH8402-01-02` → qty **92**（＝56+36，符規格範例）。
- ds04_orders 總行數與原檔一致：**1454 筆**（parse 0 unparsed；與 Task061 紀錄 1454 一致），
  外包鞋面 70 筆。
- ⚠ **11部 + 11部(2) 觀察**：解析器把 `11部` 與 `11部 (2)` 存為**兩個不同 dept 值**
  （128 / 40 筆），**dept 層未合併**；但兩者 LEAN 代碼相同（11A1/11A2/11B1/11B2/11C），
  在編制表/訂單以 LEAN 分組時會落在同一 LEAN。是否要在 dept 層合併＝待 Jim 確認
  （現況：不合併，不影響 LEAN 層彙總）。截圖 `step1_ds04.png`。

### Step 2｜覆蓋＋變更記錄（決策① 本輪實作）✅
- **本輪唯一授權的程式修改**：`database.py ds04_import()` 加入覆蓋前後數量比對 → `ds04_edit_log`。
- 作法：複製原檔，改 1 筆訂單數量（`MF2605KI5735-10`：923→**1700**，+777），重新匯入。
- 驗證：
  - 主表總筆數仍 **1454**（覆蓋、**無重複行**）；目標訂單 qty＝**1700**（新值）。
  - `ds04_edit_log` 精確出現 **1 筆** `reimport`：
    `field_name=1部/1A/KI5735/MF2605KI5735-10 qty, old_value=923, new_value=1700`。
- 截圖 `step2_ds04_after.png`。

### Step 3｜明細表確認鎖定 ✅
- POST `/api/ds04/lock {action:lock, month:2026-06}` → 鎖定成功。
- 鎖定後嘗試改單（PUT /api/ds04/order/<id>）→ 回 `ok:false「月份 2026-06 已鎖定確認，無法修改」`＝**擋下**。
- lock 狀態查詢 locked:true。（驗證後已 unlock 保持 E2E 庫乾淨）。截圖 `step3_lock.png`。

### Step 4｜/eolr-settings ✅
- 初始各 LEAN `eolr=120, is_default=true`＝**繼承預設/上月**（本庫無上月設定 → 落預設 120）。
- 修改 `1A→60` → 回讀 `eolr:60, is_default:false, updated_at` 有值＝**可修改且持久化**。
- eolrDiv：120→÷30、60→÷60 的公式在 `get_bianche_data` 以 `ΣST×eolr/3600` 實作
  （120→ST/30、60→ST/60），**程式碼層確認一致**；實際數字需 IE 標時（BLOCKED）。截圖 `step4_eolr.png`。

### Step 5｜/allocation 勾選表 ⛔ BLOCKED（無 IE 工序資料）
- 分配身分登入（`/api/allocation/login` jim/admin）成功；`/api/allocation/prefill` 回 200
  但 `headers:0, inserted:0`＝**沒有可分配的部件**。
- 原因：勾選表的部件來自 IE cutting 工序（`prefill_allocation` 讀 `ie_process`），本機無 IE 資料 → 0 筆。
- 結論：勾選/保存機制（路由、權限、後端擋跨單位）程式面正常，但**無資料可勾**，
  「各製程分區各勾 1 筆」**無法在本機執行**。需有 IE 鎖定版的機器（ME129 或補 IE 來源）才能驗。截圖 `step5_allocation.png`。

### Step 6｜/bianche 編制表計算 ⚠ PARTIAL
- **決策③ 已驗證**：所有型體 `has_locked=false / has_ie=false`、MP 欄（cutting/stitching/assembly/c2b）
  ＝`null`（前端紅底），但**訂單照列不擋單**（例：CODECHAOS 27 qty 5889、TAEKWONDO 679、GAZELLE GOLF 878）。
- **MP 手算核對 ⛔ BLOCKED**：裁斷/針車/成型/STF MP 全部依賴 IE 鎖定版標時，本機無 IE → 無法取得系統值做手算對照。
- **合併規則差異（記錄，未改）**：規格要求「同組同 Model Name **且 FOB Cutting/Stitching/Assembly 相同**」才併列；
  現行程式**只用 lean+model_name 併列**（arts 合併、訂單加總），**未比對 FOB**。屬既有邏輯差異，依規則不改，待 Jim 決定。
- 截圖 `step6_bianche.png`。

### Step 7｜導出 ✅
- `/api/bianche/export` → xlsx 17.8KB，分頁 **CSA / OCS / RB / QC**，**無核對/對照 tab**（符決策③）。
- CSA 欄：LEAN｜鞋型名稱｜ART｜訂單｜裁斷MP｜成型MP｜針車MP｜協理給｜LEAN編制。
- 抽 3 筆與畫面一致：CODECHAOS 27 qty 5889、TAEKWONDO 679、GAZELLE GOLF 878（訂單量一致；
  MP 欄兩邊皆空＝一致，因無 IE）。檔存 `test_screenshots/bianche_e2e/step7_bianche_export.xlsx`。

## 3 型體手算 vs 系統值對照表
| 型體 (LEAN 1A) | 訂單量(系統) | 裁斷MP | 針車MP | 成型MP | STF MP | 手算 |
|---|---|---|---|---|---|---|
| CODECHAOS 27 (KI5735,KK0658,KI1388,KI5736) | 5889 | null(紅) | null | null | null | ⛔ 無 IE 鎖定版，無法手算 |
| ADIDAS TAEKWONDO LACE W (IH1969,HQ7583,KH7849,KH7850) | 679 | null(紅) | null | null | null | ⛔ 同上 |
| GAZELLE GOLF (KJ8220,KJ5427,KJ5428,KJ5429) | 878 | null(紅) | null | null | null | ⛔ 同上 |

> MP 手算需 IE 鎖定版標時（`裁斷MP=ΣST×eolr/3600−勾走`、`STF MP=訂單×TCT/3600/222` 等）。
> 本機無 IE 來源，依「不造測試資料」規則記為 BLOCKED。**建議**：在有 IE 鎖定資料的機器
> （ME129 或補 `IE_xlsx` 來源後）重跑 Step5/6 即可完成 MP 手算核對。

## 截圖清單（test_screenshots/bianche_e2e/）
`step1_ds04.png`、`step2_ds04_after.png`、`step3_lock.png`、`step4_eolr.png`、
`step5_allocation.png`、`step6_bianche.png`、`step7_bianche_export.xlsx`、`e2e_results.json`（完整 API 回應）。

---

# Task B｜裁斷合併 bug 診斷（只診斷，不修）

完整診斷見 `test_screenshots/cutting_merge_diag/DIAGNOSIS.md`。材料：01–05 截圖 + diag_data.json + unsaved_edit_test.json。

## 真因定位
**真因＝合併觸發的整段重載 `loadSegment()` 丟棄「未提交」的層/件/刀編輯**（已重現，LOST=true）。
- 層/件/刀輸入格（`ie_cell_detail.html` `renderCuttingRow`/`edMA` ~L908）只有 **blur 才存**
  （`onblur=commitEditStatic`；`oninput=recalcCuttingStdByPid` 只重算不存）。
- `submitMerge()`(~L1708) / `saveGroupHc()`(L1650) / `unmergeGroup()`(L1664) 存檔後呼叫
  `loadSegment(SEG)`(L751)：`EDITS={}` + 重新 fetch + `renderZones` 整段重繪 → 未 blur 的輸入被伺服器舊值覆蓋。
- 重現：層數輸入 `99`（不 blur）→ 合併重載後變回 `2`（`unsaved_edit_test.json`: LOST=true）。

## 反證：已儲存資料的合併渲染是**正確**的（不是渲染錯位）
- 連續合併(seq1,2,3) / 非連續合併(seq1,3) 前後，層/件/刀值全數保留；
  群組首列出 rowspan 群組格、非首列 td 少 1（實際人數欄被 rowspan 蓋，正確）；非連續時成員自動重排相鄰以對齊 rowspan。
- 即 rowspan / 群組列 / 欄位對齊**無缺陷**（推測 Task086/087 rowspan 重寫已修純渲染面）。

## 修復方案建議（未改任何程式碼，等 Jim 拍板）
- **A 最小**：`submitMerge`/`saveGroupHc`/`unmergeGroup` 在 `loadSegment` 前先 `blur()` + await 進行中的 commit，確保先存再重載。
- **B 治本**：合併成功後不整段重載，只局部更新受影響 zone 的 tbody（插入 group 格），保留其他未存輸入。
- **C 防呆**：層/件/刀改 `input` 去抖動即時存，或 dirty 追蹤＋重載前自動提交。

---

# Task C｜IE 導出 29 欄彙總表 ⛔ BLOCKED（規格檔缺失）

## 結論：無法實作，規格檔本機不存在
- 規格檔 `IE产能导入格式规格_20260625.md` **遍尋不著**：
  - `git ls-files`（版控清單）：無。
  - `git log --all`（歷史任何時點）：從未存在於本 repo。
  - 全庫 `.md` grep「产能/產能/导入格式/29欄/彙總表」：只命中本報告與無關的 MP 勾選 prompt。
  - 磁碟（Desktop / OneDrive / Documents / Downloads / 全 C:\）掃描「产能 / 20260625 / 导入格式」：無。
- 依無人值守規則（**不猜、不造測試資料、逐欄須與規格一致**）：**不臆造 29 個欄名**去實作，
  否則產出的欄位無法保證與規格一致，反而製造錯誤基準。故記為 BLOCKED，不動程式碼。

## 現況基準（供 Jim 對照）
- 既有 `/api/ie/export/<header_id>`（app.py L309）已有一個 **「MP 彙總」分頁，但只有 7 欄**：
  `EOLR｜ART 列表｜裁斷MP｜針車MP｜成型MP｜庫存MP｜來源`（值取自 `ob_epph`）。
- 這**不是**本任務要的 29 欄「IE 產能彙總表」；兩者欄數/用途不同。

## 解除封鎖後的建議做法（等 Jim 提供規格）
1. Jim 提供 `IE产能导入格式规格_20260625.md`（或指出其來源 xlsx / 路徑）。
2. 依規格逐欄定義 29 欄 → 新增 `/api/ie/export/summary`（彙總表，全型體一張表）。
3. 型體明細表格式未定 → 不做，僅在導出選單預留一個 disabled 入口（避免未定規格先落地）。
4. Playwright 驗證：導出檔存在、29 欄欄名逐欄對規格、抽 3 型體數值核對。
5. 標記「待 Jim 驗收」。

> 未新增臆測性 UI 入口（規格未定前先落地介面違反「規格＝唯一真相」原則）。等規格到位再一次做完。

---

# 自行決定清單（無人值守）

| # | 哪一步 | 遇到什麼問題 | 採用的預設值/選項 | 為什麼 |
|---|---|---|---|---|
| 1 | Task A 測試檔 | DS-04 上次驗收原檔路徑（OneDrive）本機不存在 | 改用 repo 內 `data/source_files/廠務編制/2026年6月份正式进度表 5 30.xlsx`（＝24_DATA_SYSTEM 記錄的同一份 Jun-2026 DS-04） | 是「同一份」原檔的 repo 版本，非捏造；符「找得到就用」 |
| 2 | Task A DB | 本機 `atlas.db` 是 6/13 的殘舊玩具庫（無 ds04_orders/IE/編制表/使用者表） | 用 `ATLAS_DB` 環境變數指向獨立 `atlas_e2e.db`，`init_db()` 建全新 schema | 不動既有 DB、不碰生產機（規則3）；影響最小 |
| 3 | Task A 登入 | 系統無預設 admin 帳號可登入 | 建 `jim/admin123`(admin) 於 E2E 庫 | E2E 需登入；僅在隔離庫 |
| 4 | Task A Playwright | repo 無 Playwright，Python 3.14 pip wheel 未必支援 | 裝 Node 版 chromium 於 repo 外 `C:\pw-e2e`，腳本副本進 repo | 不污染 repo node_modules；Node 版對版本最穩 |
| 5 | Task A Step1 上傳 | DS-04 無網頁上傳路由，只有 CLI | 以 CLI `import_ds04` 匯入＋Playwright 讀 /ds04 畫面驗證 | 現行系統本就 CLI 匯入；如實反映 |
| 6 | Task A Step6 | `/bianche` 因 `ie_process` 表不存在直接 500 | 建**空** IE 表（0 筆，schema-only）讓「缺 IE→紅底不擋單」路徑可跑 | 空表非業務資料；驗證決策③而不造假數字 |
| 7 | Task A Step5/6 MP | 無 IE 鎖定資料，MP 無法手算 | 記為 BLOCKED，不捏造 | 規則「不造測試資料」；如實記錄待 Jim |
| 8 | Task A Server port | 避免與任何既有服務衝突 | 用 5057（非 5000） | 影響最小、明確標示為 E2E |
| 9 | Task B 重現 | 需畫面上有 cutting 列才能重現渲染 bug；無 IE 資料 | 種**最小 UI 重現 fixture**（5 道 cutting 工序，隔離庫）| UI-bug repro 非業務資料造假；且合併是前端行為 |
| 10 | Task B 診斷 | API 驅動的合併重載未重現「消失」 | 續測「未提交編輯」情境並確認 LOST=true 為真因 | 忠實反映：已存資料渲染正確，未存編輯才丟 |
| 11 | Task C | 規格檔 20260625 本機不存在 | 記 BLOCKED，不臆造 29 欄、不動碼、不加臆測 UI | 規則「不猜、規格＝唯一真相」 |

---

## 三次 push 狀態
- **Task A**：✅ push 成功（commit `3a8fda1`）— 編制表 E2E + 決策①②③
- **Task B**：✅ push 成功（commit `3b3e939`）— 裁斷合併 bug 診斷報告
- **Task C**：⛔ 無程式產出（規格缺失）；本報告 Task C 章節隨最終 commit 一起 push

## 截圖/產出清單
- `test_screenshots/bianche_e2e/`：step1–7 截圖 + e2e_results.json + step7_bianche_export.xlsx
- `test_screenshots/cutting_merge_diag/`：01–05 截圖 + DIAGNOSIS.md + diag_data.json + unsaved_edit_test.json
- 重現腳本（repo 內）：`flask_backend/tests/e2e_taskA_data.py`、`e2e_bianche_playwright.js`、
  `e2e_taskB_seed.py`、`e2e_taskB_cutting_merge_diag.js`、`e2e_taskB_cutting_merge_unsaved.js`

---

# 總結（給 Jim 8 小時後）
- **Task A ✅ 大部分完成**：Step1/2/3/4/7 PASS（決策①已實作並驗證：覆蓋+變更記錄）；
  Step5（勾選表）與 Step6（MP 手算）因**本機無 IE 來源資料**而 BLOCKED（決策③紅底不擋單已結構驗證）。
  → 待 Jim：在有 IE 鎖定資料的機器（ME129 或補 IE_xlsx）重跑 Step5/6 完成 MP 手算核對。
- **Task B ✅ 完成診斷**：真因＝合併觸發 `loadSegment()` 整段重載丟棄未提交的層/件/刀編輯（已重現）；
  渲染/rowspan 本身正確。修復建議 A/B/C 待 Jim 拍板（未改碼）。
- **Task C ⛔ BLOCKED**：規格檔 `IE产能导入格式规格_20260625.md` 本機不存在（repo/git/磁碟皆無）。
  → 待 Jim：提供規格檔或來源路徑，即可實作 29 欄彙總表導出。
- **本機關鍵限制**：`data/source_files/IE_xlsx/`（623MB IE 來源）不在本 Code 機 → 所有需 IE 鎖定資料的驗證受阻。

---

# Task S 驗收（2026-07-14）：IE表/編制表 最外層主頁簽整合

**定案依據**：21_CURRENT_STATUS「看編制的人也要查 IE 流程 → 最外層兩個主頁簽切換（IE表/編制表）」。

**實作**：共用外框 `/app`（新檔 `app_shell.html`）以兩個 iframe 各載 `/ie`、`/bianche`。
`ie_interface.html`、`bianche.html` **零改動**（`git diff --stat` 空）→ 兩頁全部函式/按鈕逐一保留，零迴歸「by construction」。
異動僅：`app_shell.html`(新)、`flask_backend/app.py`(+`/app` 路由)、`login.html`(登入導 `/app#ie`)、`test_task_s_shell.py`(新測試)。

**Playwright 隔離自測**（`serve_test_isolated.py` @ 5099，DB=atlas_test.db 一致性副本，正式庫零觸碰）：

| # | 驗收項 | 結果 | 實測 |
|---|--------|------|------|
| 1 | 登入預設進 IE表 | ✅ | ie_frame active + 頁簽亮 + `#ie` |
| 2 | 兩頁簽互切 10 次、各自表格渲染 | ✅ | 10 輪皆 OK（IE `#ie-tbody` / 編制 `#unitTable`）|
| 3 | IE 細表開啟 | ✅ | jim 見編輯格 `input.cell-inp` |
| 4 | 編制表功能抽測 | ✅ | 匯入入口+匯出鈕+單位表 13 列 |
| 5 | 細表未存值切頁簽 flush 保住 | ✅ | 切前 pending≥1 → 切後 pending=0、值 `77.7` 保留（不靜默丟）|
| 6 | 三語切換 | ✅ | IE：鞋型/Model Name/Tên giày 三語有效；編制表照渲染不破 |
| 7 | F5 停留原頁簽 | ✅ | `#bianche`→bianche、`#ie`→ie |
| 8 | tongcai（read_only）兩頁全灰 | ✅ | IE `input.cell-inp`=0（Task I 替換為 span）；編制表 191 格全 disabled |
| 9 | editor 權限迴歸 | ✅ | 指派 header 可編（210 格）+ 未指派 `can_edit=False` + 編制表唯讀 |
| 10 | 舊入口零迴歸 | ✅ | `/ie`、`/bianche` 仍可獨立開 |

**結果：10/10 PASS。**

**工具列不混排說明**：各頁工具列在自己 iframe 內，切到哪頁只見該頁工具列，外框頂欄僅兩個主頁簽，天然不混排。
**三語補註**：`bianche.html` 現況本無語言切換鈕（純中文），Task S 未新增（屬另案）；驗收「三語兩頁正常」＝IE 三語有效且編制表不因語言操作而破版。
**自行決定清單**：外聯單位 tongcai/dianno/dacu 登入仍導 `/allocation`（其專用勾選工具，不動）；shell 可由任何已登入帳號經 `/app` 進入，故 read_only 全灰仍可驗。

---

# Task T 驗收（2026-07-14）：功能權限矩陣

**設計（中樞代決，Jim 可推翻）**：7 單元＝IE編輯/勾選部件/撥人/匯入/導出/審核/基礎資料。
`sys_users.permissions`(JSON) 儲存矩陣；`NULL`＝依角色預設（遷移零變化），非 NULL＝admin 明確設定的權威矩陣。
admin 永遠全部。雙層 403：閘門端點 `既有角色 OR 矩陣`（`_unit_allowed`）；開放端點 `_matrix_block`（只擋明確矩陣帳號、舊帳號 fall-through）。

**代表端點（7 單元各一）**：ie_edit=`/api/ie/cell/save`、select_parts=`/api/allocation/check`、
allocate=`/api/allocation/parts`、import=`/api/ie/import/apply`、export=`/api/ie/export/capacity`、
audit=`/api/ie/review/queue`、base_data=`POST /api/equipment_types`。

**Playwright 隔離自測（5099）**：

| # | 驗收項 | 結果 | 實測 |
|---|--------|------|------|
| 1 | 只勾撥人帳號 `/api/me/units` | ✅ | `['allocate']` |
| 2 | 只勾撥人：其他 6 單元 API=403、撥人≠403 | ✅ | ie_edit/select_parts/import/export/audit/base_data=403；allocate=200 |
| 3 | admin 不受矩陣限 | ✅ | 6 端點皆 200 |
| 4 | manager 零變化 | ✅ | 審核=200 基礎資料=200 導出=200 IE編輯=403 |
| 5 | editor(指派) 零變化 | ✅ | IE編輯=200 審核=403 導出=403 |
| 6 | read_only(tongcai) 零變化 | ✅ | 閘門單元=403、撥人(有alloc身分)=200 |
| 7 | 帳號管理頁矩陣 UI 存取一致 | ✅ | 勾 import 存→DB `['allocate','import']` |

**結果：8/8 PASS。**

**前端入口隱藏**：現有頁面本已依角色隱藏敏感入口（read_only 帳號本就看不到 IE編輯/導出/審核/設備種類）→「未勾＝入口不顯示」對 read_only 基底帳號 by construction 成立、零迴歸；`/api/me/units` 已備供未來逐頁更細分隱藏。
**自行決定清單**：`select_parts↔/allocation/check`、`allocate↔/allocation/parts` 的細分對映為中樞代決（原碼未區分二者，Jim 可調整端點歸屬）。

---

# Task V 驗收（2026-07-14）：編制表 Step5/6 合成資料邏輯層全驗證

**方法**：不等真檔。deterministic 合成 IE 世界注入隔離 E2E 庫 `atlas_v_e2e.db`；**期望值由腳本獨立公式另算**（不呼叫被測程式），再與 db 函式輸出逐一比對。
合成＝20 型體全段標時（EOLR 60/120 各半、裁斷連刀≠1、手工/公式列混合）+ 3 缺 IE 型體 + 1 offline 勾選承接。

| # | 驗收項 | 結果 | 期望 vs 實際（樣本） |
|---|--------|------|----------------------|
| 1 | Step6 MP 20 型體 `get_bianzhi_detail` 逐欄==獨立期望 | ✅ 0 差異 | VMODEL-01(eolr120)：裁斷 9.0/9.0、針車 3.0/3.0、成型 5.0/5.0、K 17.0/17.0、C2B 17.0/17.0 |
| 2 | 裁斷連刀÷N 公式 | ✅ | 連刀4：獨立 180.0 == `_recalc_new_std` 180.0；連刀1=720.0（÷連刀生效）|
| 3 | offline 撥人路徑 | ✅ | VMODEL-02：q_ext 2.0/2.0、C2B=K+moved=17.0/17.0 |
| 4 | 缺 IE 紅底不擋單（決策③）| ✅ | 3 型體 has_locked=False、MP=None、訂單量保留（不丟單）|
| 5 | STF 式 訂單÷(3600÷TCT)÷222 | ✅ | order 6660／tct 222 → 1.85（對照 get_allocation_parts 打粗水洗）|
| 6 | 36欄導出：已知人數欄真填值逐欄比對 | ✅ | 20/20 裁断/针车/成型/CSA 标准人数==獨立期望；CT/產能/PPH 未知欄仍留空（不臆造）|
| 7 | 一條龍 E2E（Playwright /bianche）| ✅ | 合成 lean 渲染 + 未鎖定紅底 + 導出 200 |

**結果：7/7 PASS。**

**程式異動**：`export_ie_capacity()` 新增填「已知人數欄」（裁断/针车/成型/CSA 标准人数，來源＝鎖定版 IE，與 Step6 同源公式）；有鎖定版才填、否則留空 → 對真資料零迴歸。CT/產能/PPH/Layout 等欄公式出自已丟失的『數據源-IE标准』規格，維持留空（不臆造）。
**G-03 降級**：BLOCKED 之「邏輯層」已解（Task V 全驗證），僅剩真資料層（真 IE xlsx 對映覆蓋 + 廠務檔逐欄比對 + 未知欄公式）待 Jim 供檔。

---

# Task W 驗收（2026-07-14）：SmartPN 品牌端 V3 KPI Dashboard

**範圍（43 號遺留、中樞代決）**：`docs/preview/SMARTPN_DEMO_V3.html` 加 Dashboard 頁；5 KPI 全由 MOCK_WORLD 推導；不含毛利率；遵隱私；EN/ZH；引導 +1 步；**疊加不重寫**（不動 v1 函式）。

| # | 驗收項 | 結果 | 實測 |
|---|--------|------|------|
| 1 | Dashboard 入口 + 5 KPI 卡 | ✅ | nav 鈕 + page active + 5 卡 |
| 2 | KPI 全由 MOCK_WORLD 推導（Brand-A==獨立期望）| ✅ | visible5 / grants3 / mapping33% / evidence3 / dpp79% |
| 3 | 帳號切換 A→B 可見數變 | ✅ | A=5 → B=3（選擇性開放）|
| 4 | EN/ZH 雙語 | ✅ | 'Materials visible'↔'可見材料數' |
| 5 | 不含毛利率 + 不露他方私密 | ✅ | KPI 卡 0 margin 指標；他方名/私密值 0 洩露 |
| 6 | 引導腳本 +1 步（6 步含 Dashboard）| ✅ | GUIDE.length=6、⑥ Dashboard |
| 7 | 功能迴歸 v3 全函式（V1_PARITY 缺一即 FAIL）+ 測試鉤子 | ✅ | V1_PARITY 0 缺；hooks（+getDashboardKPIs）齊 |
| 8 | 全程操作 0 pageerror | ✅ | 0 |

**結果：8/8 PASS。** KPI 定義：可見材料數=visibleTo(ACCT)；授權中請求=可見材料含 granted 欄位數；Mapping 進度=signed/total；交換存證量=evidenceRecords 數；DPP 就緒度=可見材料欄位完備率均值。
**隱私**：僅出彙總數字，KPI 卡不含毛利率，不列交易對手名/私密欄位值。
